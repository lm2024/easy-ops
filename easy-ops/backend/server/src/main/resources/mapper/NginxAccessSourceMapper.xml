<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ops.server.mapper.NginxAccessSourceMapper">

    <select id="findAll" resultType="com.ops.common.model.NginxAccessSourceModel">
        SELECT * FROM nginx_access_source ORDER BY id DESC
    </select>

    <select id="findById" resultType="com.ops.common.model.NginxAccessSourceModel">
        SELECT * FROM nginx_access_source WHERE id = #{id}
    </select>

    <select id="findByNodeId" resultType="com.ops.common.model.NginxAccessSourceModel">
        SELECT * FROM nginx_access_source WHERE node_id = #{nodeId} ORDER BY id DESC
    </select>

    <select id="findEnabledByNodeId" resultType="com.ops.common.model.NginxAccessSourceModel">
        SELECT * FROM nginx_access_source WHERE node_id = #{nodeId} AND enabled = 1 ORDER BY id ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO nginx_access_source (
            node_id, name, log_path, log_format, enabled, slow_threshold_sec,
            max_keys_per_minute, last_offset, last_inode, last_report_time, last_error,
            create_time, update_time
        ) VALUES (
            #{nodeId}, #{name}, #{logPath}, #{logFormat}, #{enabled}, #{slowThresholdSec},
            #{maxKeysPerMinute}, #{lastOffset}, #{lastInode}, #{lastReportTime}, #{lastError},
            #{createTime}, #{updateTime}
        )
    </insert>

    <update id="update">
        UPDATE nginx_access_source
        SET node_id = #{nodeId},
            name = #{name},
            log_path = #{logPath},
            log_format = #{logFormat},
            enabled = #{enabled},
            slow_threshold_sec = #{slowThresholdSec},
            max_keys_per_minute = #{maxKeysPerMinute},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM nginx_access_source WHERE id = #{id}
    </delete>

    <update id="updateReportStatus">
        UPDATE nginx_access_source
        SET last_report_time = #{lastReportTime},
            last_error = #{lastError},
            update_time = #{lastReportTime}
        WHERE id = #{id}
    </update>
</mapper>
