<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ops.server.mapper.ProjectMapper">
    <select id="findById" resultType="com.ops.common.model.ProjectModel">
        SELECT * FROM project_info WHERE id = #{id}
    </select>
    <select id="findByName" resultType="com.ops.common.model.ProjectModel">
        SELECT * FROM project_info WHERE name = #{name}
    </select>
    <select id="findByFilters" resultType="com.ops.common.model.ProjectModel">
        SELECT * FROM project_info
        <where>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <!-- (Task 5) 修复 nodeIds 模糊匹配：FIND_IN_SET + CAST 避免 H2 类型转换错误 -->
            <if test="nodeId != null">
                AND (FIND_IN_SET(CAST(#{nodeId} AS VARCHAR), node_ids) > 0
                     OR node_ids = CAST(#{nodeId} AS VARCHAR))
            </if>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE '%' || #{keyword} || '%'
                     OR jar_name LIKE '%' || #{keyword} || '%')
            </if>
        </where>
        ORDER BY create_time DESC
        <if test="pageSize != null">
            LIMIT #{pageSize} OFFSET (#{page} - 1) * #{pageSize}
        </if>
    </select>
    <select id="countByFilters" resultType="long">
        SELECT COUNT(*) FROM project_info
        <where>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <!-- (Task 5) 修复 nodeIds 模糊匹配 -->
            <if test="nodeId != null">
                AND (FIND_IN_SET(CAST(#{nodeId} AS VARCHAR), node_ids) > 0
                     OR node_ids = CAST(#{nodeId} AS VARCHAR))
            </if>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE '%' || #{keyword} || '%'
                     OR jar_name LIKE '%' || #{keyword} || '%')
            </if>
        </where>
    </select>
    <select id="findByFiltersInTenant" resultType="com.ops.common.model.ProjectModel">
        SELECT * FROM project_info
        <where>
            AND tenant_id = #{tenantId}
            <if test="projectIds != null and projectIds.size() > 0">
                AND id IN <foreach collection="projectIds" item="projectId" open="(" separator="," close=")">#{projectId}</foreach>
            </if>
            <if test="status != null and status != ''">AND status = #{status}</if>
            <if test="nodeId != null">AND (FIND_IN_SET(CAST(#{nodeId} AS VARCHAR), node_ids) &gt; 0 OR node_ids = CAST(#{nodeId} AS VARCHAR))</if>
            <if test="keyword != null and keyword != ''">AND (name LIKE '%' || #{keyword} || '%' OR jar_name LIKE '%' || #{keyword} || '%')</if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{pageSize} OFFSET (#{page} - 1) * #{pageSize}
    </select>
    <select id="countByFiltersInTenant" resultType="long">
        SELECT COUNT(*) FROM project_info
        <where>
            AND tenant_id = #{tenantId}
            <if test="projectIds != null and projectIds.size() > 0">AND id IN <foreach collection="projectIds" item="projectId" open="(" separator="," close=")">#{projectId}</foreach></if>
            <if test="status != null and status != ''">AND status = #{status}</if>
            <if test="nodeId != null">AND (FIND_IN_SET(CAST(#{nodeId} AS VARCHAR), node_ids) &gt; 0 OR node_ids = CAST(#{nodeId} AS VARCHAR))</if>
            <if test="keyword != null and keyword != ''">AND (name LIKE '%' || #{keyword} || '%' OR jar_name LIKE '%' || #{keyword} || '%')</if>
        </where>
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO project_info (name, tenant_id, project_type, node_ids, start_script, stop_script, restart_script, jvm_opts, env_vars, jar_name, deploy_dir, frontend_dir_name, frontend_deploy_dir, health_check_enabled, health_check_port, health_check_path, health_check_keyword, monitor_interval_sec, status, create_time, update_time)
        VALUES (#{name}, #{tenantId}, #{projectType}, #{nodeIds}, #{startScript}, #{stopScript}, #{restartScript}, #{jvmOpts}, #{envVars}, #{jarName}, #{deployDir}, #{frontendDirName}, #{frontendDeployDir}, #{healthCheckEnabled}, #{healthCheckPort}, #{healthCheckPath}, #{healthCheckKeyword}, #{monitorIntervalSec}, #{status}, #{createTime}, #{updateTime})
    </insert>
    <update id="update">
        UPDATE project_info SET name = #{name}, tenant_id = #{tenantId}, project_type = #{projectType}, node_ids = #{nodeIds}, start_script = #{startScript},
        stop_script = #{stopScript}, restart_script = #{restartScript}, jvm_opts = #{jvmOpts},
        env_vars = #{envVars}, jar_name = #{jarName}, deploy_dir = #{deployDir},
        frontend_dir_name = #{frontendDirName}, frontend_deploy_dir = #{frontendDeployDir},
        health_check_enabled = #{healthCheckEnabled}, health_check_port = #{healthCheckPort},
        health_check_path = #{healthCheckPath}, health_check_keyword = #{healthCheckKeyword},
        monitor_interval_sec = #{monitorIntervalSec},
        status = #{status}, update_time = #{updateTime}
        WHERE id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM project_info WHERE id = #{id}
    </delete>
    <select id="getNodeIdsById" resultType="string">
        SELECT node_ids FROM project_info WHERE id = #{id}
    </select>
</mapper>
