<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ops.server.mapper.FileAccessLogMapper">
    <insert id="insert">
        INSERT INTO file_access_log (user_id, node_id, file_type, file_path, action, content_summary, ip, create_time)
        VALUES (#{userId}, #{nodeId}, #{fileType}, #{filePath}, #{action}, #{contentSummary}, #{ip}, #{createTime})
    </insert>

    <delete id="deleteBefore">
        DELETE FROM file_access_log WHERE create_time &lt; #{cutoff}
    </delete>
</mapper>
