# 2026-08-14

## nginx 流量监控"看不到日志"根因诊断

- 数据链路确认：**Agent 上报,Server 不读文件**。Server 只存"日志源配置"(logPath+nodeId),Agent 每 30s `GET /nginx-traffic/agent/sources` 拉本节点启用源 → 每 2s 增量 tail `logPath`(OffsetTracker 记 offset+inode) → 分钟桶聚合 → 每 60s `POST /nginx-traffic/ingest` 上报 → Server 写 `nginx_minute_stat`/ip/dimension 表 → 前端查询。
- 测试环境当前状态（已实测）：
  1. **Server 8081 已停**。server.log 末条 `2026-08-13 14:52:09 Shutting down Quartz Scheduler`，lsof 8081 无监听。三个 agent 全部 `Connection refused`。
  2. **nginx-demo 环境清空**：`ops-nginx-demo` 容器、`easyops-nginx-demo` 镜像、agent 内 `/app/data/apps/` 均不存在；`backend_nginx-access-logs` 卷为空（无 access.log）。
  3. agent 容器时区为 UTC（比宿主机 CST 慢 8h），docker logs 时间戳需 +8h 解读。
- 待办：用户重启 8081 → 重建 nginx-demo（docker-compose up）→ 核对日志源 logPath=容器内 `/var/log/nginx/access.log` + nodeId=agent-1(3)。
- 提醒：08-08 记忆里"已部署 nginx-demo/demo-test-app"已不成立，环境被重置过。
