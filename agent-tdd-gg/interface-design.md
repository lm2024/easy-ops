# 通过 TDD 驱动接口设计

TDD 不仅仅是一种测试技术，更是一种**设计工具**。

## ## 设计指导原则
1.  **作为第一位使用者**：先写测试迫使你从**调用者（使用者）**的角度去思考，而不是从实现者的角度去思考。这自然会带来更干净、更内聚的公共接口。
2.  **迪米特法则 (Law of Demeter)**：如果编写测试需要非常复杂的初始化代码（需要组装 5 个以上的依赖对象），说明你的类设计耦合度过高，需要拆分。
3.  **高内聚，低耦合**：测试应该促使你设计出单一职责的类。如果一个类的测试文件包含了多个毫不相关的业务场景，说明这个类承载了太多职责。